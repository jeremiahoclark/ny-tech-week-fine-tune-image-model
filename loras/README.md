# LoRA Artifacts

Trained LoRA checkpoints are written to:

```text
outputs/lora/
```

Do not commit generated `.safetensors` files into the public repo. If a checkpoint needs to be shared before the workshop, put it in external storage or Git LFS deliberately.
